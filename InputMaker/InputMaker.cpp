#include <raylib.h>

int main()
{
    InitWindow(400, 400, "Input Maker");

    while (!WindowShouldClose())
    {
        BeginDrawing();

        EndDrawing();
    }

    CloseWindow();

    return 0;
}
